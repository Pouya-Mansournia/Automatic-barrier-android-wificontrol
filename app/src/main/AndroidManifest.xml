<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ir.pasargad.pouyamansournia.barrier">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">

        <activity android:name="ir.pasargad.pouyamansournia.barrier.Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.DSYYY" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name="ir.pasargad.pouyamansournia.barrier.Activities.SplashST">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="ir.pasargad.pouyamansournia.barrier.Activities.Setting" />


        ///////////////////// NMASMART:159753//////////////////////////
    </application>

</manifest>
